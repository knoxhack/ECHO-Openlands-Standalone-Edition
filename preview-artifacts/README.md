# Preview Artifacts

These artifacts are committed so the repository contains the visible Openlands Standalone preview payload.

They are compiled preview artifacts, not approved player-facing release files. Do not publish them through launcher channels or GitHub Releases until the remaining public-alpha gates pass.
